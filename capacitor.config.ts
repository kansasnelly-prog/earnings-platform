import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sreymara.app',
  appName: 'SREYMARA',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
